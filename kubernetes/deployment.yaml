apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask_hello
  template:
    metadata:
      labels:
        app: flask_hello
    spec:
      containers:
      - name: flask_hello
        image: 127.0.0.1:49452/flask_hello:latest
        ports:
        - containerPort: 5000
